import { createUseStyles } from '~/provider/theme';

export const useStyle = createUseStyles({
  root: {
    marginBottom: '1rem',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'flex-end',
    gap: '0.375rem',
  },
});
